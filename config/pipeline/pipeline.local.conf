# =============================================================================
# Local Dev Pipeline Configuration
# =============================================================================
# NOT committed to git (see .gitignore)
# =============================================================================

VALHALLA_ENV=local

# Paths (WSL2 mount paths)
VALHALLA_TILE_DIR=/mnt/c/Users/Vibin/Workspace/valhallaV3/data/valhalla_tiles
OSM_DIR=/mnt/c/Users/Vibin/Workspace/valhallaV3/data/osm
VALHALLA_ADMIN_DIR=/mnt/c/Users/Vibin/Workspace/valhallaV3/data/admin_data
VALHALLA_LOG_DIR=/mnt/c/Users/Vibin/Workspace/valhallaV3/logs

# Build options
SKIP_ELEVATION=true
CLEAN_BUILD=false

# S3 — disabled locally
S3_TILE_BUCKET=
S3_REGION=ap-southeast-1

# Use binary built from source
# VALHALLA_BUILD_TILES_BIN is only for the local case where you compiled from source and the binary isn't on PATH. For every other environment, leave it empty and Docker handles it.
VALHALLA_BUILD_TILES_BIN=/mnt/c/Users/Vibin/Workspace/valhallaV3/build/src/valhalla_build_tiles
VALHALLA_DOCKER_IMAGE=ghcr.io/valhalla/valhalla:latest
