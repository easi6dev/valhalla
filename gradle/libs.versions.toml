# ============================================
# Valhalla JNI Dependencies Version Catalog
# ============================================
# Centralized dependency version management using Gradle Version Catalog
# Benefits:
# - Single source of truth for all versions
# - Easy to update versions across the project
# - Type-safe dependency accessors
# - Better IDE support
#
# Usage in build.gradle.kts:
#   dependencies {
#       implementation(libs.kotlin.stdlib)
#       implementation(libs.kotlinx.coroutines.core)
#   }
# ============================================

# NOTE: Kept in parity with src/bindings/java/gradle/libs.versions.toml.
# The :java-bindings subproject is built two ways — via this root catalog
# (IDE / root `valhalla` build) and via its own standalone catalog (CLI / build
# scripts). Both must define every accessor build.gradle.kts references, at
# matching versions, or one context fails to resolve. Update both together.
[versions]
# Language versions
kotlin = "2.0.21"
java = "17"

# Core dependencies
kotlinx-coroutines = "1.9.0"
slf4j = "2.0.16"
logback = "1.5.12"
json = "20240303"

# Testing
junit = "5.11.3"
assertj = "3.26.3"

# Build tools
gradle-kotlin-dsl = "4.4.1"
dokka = "1.9.20"

# Code quality
detekt = "1.23.7"

# Performance
jmh = "1.37"
jmh-gradle = "0.7.2"

[libraries]
# Kotlin
kotlin-stdlib = { module = "org.jetbrains.kotlin:kotlin-stdlib", version.ref = "kotlin" }
kotlin-reflect = { module = "org.jetbrains.kotlin:kotlin-reflect", version.ref = "kotlin" }
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-jdk8 = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-jdk8", version.ref = "kotlinx-coroutines" }

# Logging
slf4j-api = { module = "org.slf4j:slf4j-api", version.ref = "slf4j" }
slf4j-simple = { module = "org.slf4j:slf4j-simple", version.ref = "slf4j" }
logback-classic = { module = "ch.qos.logback:logback-classic", version.ref = "logback" }

# JSON
json = { module = "org.json:json", version.ref = "json" }

# Testing
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }
kotlin-test-junit = { module = "org.jetbrains.kotlin:kotlin-test-junit5", version.ref = "kotlin" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
junit-jupiter-api = { module = "org.junit.jupiter:junit-jupiter-api", version.ref = "junit" }
junit-jupiter-engine = { module = "org.junit.jupiter:junit-jupiter-engine", version.ref = "junit" }
junit-jupiter-params = { module = "org.junit.jupiter:junit-jupiter-params", version.ref = "junit" }
assertj-core = { module = "org.assertj:assertj-core", version.ref = "assertj" }

# Performance benchmarking
jmh-core = { module = "org.openjdk.jmh:jmh-core", version.ref = "jmh" }
jmh-generator-annprocess = { module = "org.openjdk.jmh:jmh-generator-annprocess", version.ref = "jmh" }

[bundles]
# Common dependency bundles
kotlin = ["kotlin-stdlib", "kotlin-reflect", "kotlinx-coroutines-core"]
logging = ["slf4j-api", "logback-classic"]
testing = ["junit-jupiter-api", "junit-jupiter-engine", "junit-jupiter-params", "kotlin-test"]
jmh = ["jmh-core", "jmh-generator-annprocess"]
[plugins]
# Kotlin
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }

# Documentation
dokka = { id = "org.jetbrains.dokka", version.ref = "dokka" }

# Code quality
detekt = { id = "io.gitlab.arturbosch.detekt", version.ref = "detekt" }

# Performance
jmh = { id = "me.champeau.jmh", version.ref = "jmh-gradle" }
